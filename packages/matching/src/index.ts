// Deterministic constraint solver + explainable scorer. Built in kickoff Session 7 — see
// ../../KICKOFF-PROMPT.md. Hard constraints filter candidates; score components are always
// returned separately, never blended into one opaque number.
export {};
