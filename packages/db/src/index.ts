// Drizzle schema, migrations, and row-level-security policies for PostgreSQL 16 + PostGIS.
// Built in kickoff Session 4 — see ../../KICKOFF-PROMPT.md. Tenant isolation is enforced at
// the database layer via RLS, not only in application code.
export {};
